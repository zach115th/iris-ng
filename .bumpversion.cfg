[bumpversion]
current_version = IRIS-NG-v1.2.3
commit = True
tag = True
parse = (?P<major>\d+)\.(?P<minor>\d+)\.(?P<patch>\d+)(-(?P<release>.*)-(?P<rebuild>\d+))?(\+iris-ng\.(?P<build>\d+))?
serialize =
	{major}.{minor}.{patch}-{release}-{rebuild}+iris-ng.{build}
	{major}.{minor}.{patch}+iris-ng.{build}
	{major}.{minor}.{patch}-{release}-{rebuild}
	{major}.{minor}.{patch}

[bumpversion:part:release_name]
first_value = regular
optional_value = regular
values = 
	alpha
	beta
	rc
	test
	regular

[bumpversion:file:source/app/configuration.py]
search = IRIS_VERSION = "{current_version}"
replace = IRIS_VERSION = "{new_version}"

[bumpversion:file:README.md]
search = v{current_version}
replace = v{new_version}

[bumpversion:file:docker-compose.yml]
search = -v{current_version}
replace = -v{new_version}
